# Roadmap

Open work is a checkbox. Shipped work moves to `docs/changelog.md`.

## Now

- [ ] First setup by a stranger on Windows 11 through the agent path, with the doctor output and probe JSON attached to the report. Until then Windows is documented, not proven.
- [ ] First setup on CachyOS on an independent machine, with the same evidence.
- [ ] Voice fit: the current presets read as “narrator”, not “small robot”. Measure pitch and playback processing for first-audio impact.

## Next

- [x] Optional offline library plug-in: ask a local Kiwix server before the web and cite passages. Shipped in the 2026-09-13 engine sync.
- [x] Actions plug-in, scoped to reminders and timers with receipts. Shipped in the 2026-09-13 engine sync.
- [x] Desktop corner presence with a Wayland overlay and hold-to-talk binding. Shipped in the 2026-09-13 engine sync.
- [ ] `gemma4:4b` and CPU-only reference measurements in `docs/evidence/`, so the tier table has readings for small machines instead of estimates.
- [ ] Non-NVIDIA VRAM reading in the doctor, including ROCm and Intel, so the suggestion is not RAM-only there.

## Later

- [ ] Home-city setting for weather, so the freshness feed does not depend on placeholder coordinates.
- [ ] Fixed capability answer for privacy questions, kept in one tested capability card instead of being regenerated by the model.
- [ ] Windows reminders, with a Task Scheduler backend that matches the Linux reminder receipts.
